import React from 'react';
import {ReactImageGalleryProps} from "react-image-gallery";

const CustomGallery = (props: ReactImageGalleryProps) => {
    // const prepareImages = () => {
    //
    //     console.log(images)
    //
    // }
    // prepareImages();
    return (
        <></>
        // <ImageGallery items={images}/>
    );
};

export default CustomGallery;
